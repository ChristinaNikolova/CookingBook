const category = {
  NAME_MIN_LEN: 3,
  NAME_MAX_LEN: 30,
};

const ingredient = {
  DESC_MIN_LEN: 3,
  DESC_MAX_LEN: 30,
};

const instruction = {
  DESC_MIN_LEN: 3,
  DESC_MAX_LEN: 30,
};

const recipe = {
  TITLE_MIN_LEN: 3,
  TITLE_MAX_LEN: 30,
};

const user = {
  PASSWORD_MIN_LEN: 6,
  PASSWORD_MAX_LEN: 50,
};

module.exports = {
  category,
  ingredient,
  instruction,
  recipe,
  user,
};
